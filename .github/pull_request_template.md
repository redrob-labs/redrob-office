## Summary

What this changes, in one or two sentences.

## Why

The problem it solves. Link the issue if there is one.

## Scope

- In scope:
- Out of scope:

## Testing

| Command | Result |
| --- | --- |
| `pnpm typecheck` | |
| `pnpm test` | |
| `pnpm build` | |
| `pnpm check:upstream-boundary` | |
| `pnpm check:licenses` | |

Manual verification (what you opened, clicked, and saw):

## Evidence

Screenshots or output for anything user-visible.

## Risk and rollback

What breaks if this is wrong, and how to undo it.

## Upstream

If this takes code from GenOffice, say which upstream commits, and confirm
`lastSyncedUpstream` in `upstream-base.json` was updated.

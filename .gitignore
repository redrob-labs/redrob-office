node_modules/
dist/
out/
.turbo/
*.log
.DS_Store
Thumbs.db
coverage/
*.tsbuildinfo
.env
.env.*
!.env.example
office/release/
apps/shell/release/
# Generated by tools/gen-third-party-notices.mjs before packaging (the shell
# `notices` script, run by every dist:* script and the CDN workflow). It is a
# build artifact regenerated on every release, not source.
apps/shell/build/THIRD-PARTY-NOTICES.txt
userData/
# What the app's own file tools write when a dev run points the artifact folder
# at the checkout. Documents a person made, not source.
office/artifacts/

# Measurement and experiment artifacts. tmp/ is scratch for benchmark runs and
# holds local paths and sample documents; it is never part of a release.
tmp/
bench-results/
*.jsonl

# Model weights. Downloaded to the user's app data at runtime, never committed.
*.gguf
*.bin
*.onnx

# Runtime binaries. llama.cpp backends are fetched per host at onboarding.
*.exe
*.dll
*.dylib
*.so
*.zip
*.tar.gz
llama-server
llama-server.exe

*.wav
*.mp3
*.m4a
*.flac
*.webm
smoke-*-result.json
secret-scan-report.json
.vscode/*
!.vscode/extensions.json
.idea/
.cursor/*
!.cursor/environment.json

# Agent scratch/task state. Not part of the product and not reviewed.
.agents/
